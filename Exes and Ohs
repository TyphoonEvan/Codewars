#include <string>

bool XO(const std::string& str) {
	int count = 0;
	for (int i = 0; i < str.length(); i++) {
		if (str[i] == 'x' || str[i] == 'X')
			counr++;
		if (str[i] == 'o' || str[i] == 'O')
			count--;
	}
	return count == 0;
}
